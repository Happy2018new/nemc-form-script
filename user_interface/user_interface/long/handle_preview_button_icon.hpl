/*
user_interface/long/handle_preview_button_icon(
  is_cancel: bool,
  form_name: str,
  element_index: int,
) -> bool
*/



is_cancel = {func, tuple.get(args, 0)}
form_name = {func, tuple.get(args, 1)}
element_index = {func, tuple.get(args, 2)}



if is_cancel:
  if {ref, int, -1} != 2:
    _ = {func, function.call('user_interface/long/edit_button', form_name, element_index)}
  fi
  return True
fi



if {ref, bool, -1}:
  _ = {func, function.call('user_interface/long/edit_button_icon', form_name, element_index)}
else:
  _ = {func, function.call('user_interface/long/edit_button', form_name, element_index)}
fi



return True