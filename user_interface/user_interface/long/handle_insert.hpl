/*
user_interface/long/handle_insert(
  is_cancel: bool,
  form: str,
  length: int,
) -> bool
*/



is_cancel = {func, tuple.get(args, 0)}
form = {func, tuple.get(args, 1)}
length = {func, tuple.get(args, 2)}



if is_cancel:
  if {ref, int, -1} != 2:
    // back to the main panel of long
  fi
  return True
fi



index = {ref, int, -1}
if index == length:
  // back to the main panel of long
else:
  _ = {func, function.call('user_interface/long/add', form, index)}
fi
return True