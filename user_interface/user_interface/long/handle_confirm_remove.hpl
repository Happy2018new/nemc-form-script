/*
user_interface/long/handle_confirm_remove(
  is_cancel: bool,
  form_name: str,
  element_index: int,
) -> bool
*/



is_cancel = {func, tuple.get(args, 0)}
form_name = {func, tuple.get(args, 1)}
element_index = {func, tuple.get(args, 2)}



if is_cancel:
  if {ref, int, -1} != 2:
    _ = {func, function.call('user_interface/long/remove', form_name)}
  fi
  return True
fi



if {ref, bool, -1}:
  _ = {func, function.call('user_interface/storage/long/remove_element', form_name, element_index)}
fi
_ = {func, function.call('user_interface/long/remove', form_name)}



return True