/*
user_interface/handle_confirm_remove(
  is_cancel: bool,
  form_name: str,
  form_type: int,
) -> bool
*/



is_cancel = {func, tuple.get(args, 0)}
form_name = {func, tuple.get(args, 1)}
form_type = {func, tuple.get(args, 2)}



if is_cancel:
  if {ref, int, -1} != 2:
    _ = {func, function.call('user_interface/remove', form_type)}
  fi
  return True
fi



if {ref, bool, -1}:
  _ = {func, function.call('user_interface/storage/del_form', form_name, form_type)}
fi
_ = {func, function.call('user_interface/remove', form_type)}



return True