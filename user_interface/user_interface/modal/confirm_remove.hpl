/*
user_interface/modal/confirm_remove(
  form_name: str,
  element_index: int,
) -> bool
*/



form_name = {func, tuple.get(args, 0)}
element_index = {func, tuple.get(args, 1)}



temp = {func, slices.new(False, form_name, element_index)}
code = {func, function.call('utils/as_func_string', 'user_interface/modal/handle_confirm_remove', temp)}
_ = {command, {func, strings.format('customform onsubmit "user_interface/modal/confirm_remove" "{}"', code)}}

temp = {func, slices.new(True, form_name, element_index)}
code = {func, function.call('utils/as_func_string', 'user_interface/modal/handle_confirm_remove', temp)}
_ = {command, {func, strings.format('customform oncancel "user_interface/modal/confirm_remove" "{}"', code)}}



_ = {command, 'customform show @s ~ ~ ~ @s "user_interface/modal/confirm_remove"'}
return True